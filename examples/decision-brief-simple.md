# Use the existing reporting pipeline

**Ready to advance.** The current pipeline already covers the requested report, so creating a second workflow would add maintenance without adding decision value.

The only change worth making now is to add the missing export field to the existing output contract.

## Next move

Extend the current report and verify one representative export. No new service or framework is justified.
