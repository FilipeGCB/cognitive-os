# V1.5 Development Baseline

Date: 2026-09-04
Repository: `FilipeGCB/cognitive-os`
Branch under construction: `feat/cognitive-os-v1-5`

## Authoritative state

| Field | Observed value | Evidence |
|---|---|---|
| `BASELINE_HEAD` | `41a14aa839c87609597cb0cdd3ff81cd06b61135` | `git ls-remote origin refs/heads/main`; fetched `origin/main` |
| `BASELINE_TAG` | `v1.4.0` → `fea0fa6ea765283288b835fec1a8ffc5195f29bb` | `git ls-remote origin refs/tags/v1.4.0`; fetched tag |
| `BASELINE_STATUS` | released stable v1.4.0; no V1.5 branch/PR | remote refs, GitHub PR list, release evidence |
| `BASELINE_BRANCH` | `feat/cognitive-os-v1-5` at `41a14aa` | local branch created from remote main |
| post-release branch | `feat/v1.4-public-foundation` remote currently `a4aed8f`; local checkout had unpublished `f7b690b` history | remote refs and local history |
| related proposal | `docs/v1.4.1-hardening-spec` at `58bb40f` | remote ref; design reviewed as input, not merged |

The working tree contained pre-existing, untracked `evals/runs/` evidence. It was not deleted, rewritten or included in this baseline document. Historical local runs are evidence about the maintainer environment only, not proof for the new candidate.

## Baseline verification

Executed in a detached worktree at `origin/main` (`41a14aa`):

- deterministic suite: **PASS — 76/76 tests**;
- V1.4 core manifest: **VALID — 19 cases**;
- V1.4 output manifest: **VALID — 10 cases**;
- public package guard: **PASS**;
- public PII scan: **PASS**;
- Python compile check: **PASS**;
- Decision Brief renderer smoke: **PASS**;
- V1.4 local behavioral conformance with `gemma4:26b-a4b-it-qat`: **PASS — 29/29**, zero critical failures. The grader was a separate invocation of the same model family, so this is not independent-provider proof.

The initial `python` commands were unavailable because the environment exposes `python3` only; all repository checks were repeated with `python3`.

## Gate 0 finding reproduction

| Finding | Reproduction against `41a14aa` | Resulting action |
|---|---|---|
| A — account-bound E2E consent | `run_mcp_case(..., server=None)` selected `notebooklm` from `mcp list`; a focused RED test observed a connection-test path without a consent flag | fixed: no implicit selection; account-bound MCP is blocked outside `notebooklm-check` |
| B — critical aggregate | `run-auto` returned success after a false `H14-E03` because its `all()` filtered that case; a focused RED test reproduced exit code `0` | fixed: every record returned by `run-auto`, including `H14-E03`, participates in the result |
| C — candidate binding | release workflow checked only `RELEASE_GATE: PASS`; it did not verify the evidence candidate SHA, manifest set, harness and artifact hashes against the workflow SHA | implementation required: machine release-evidence schema and verifier |
| D — textual contracts | no executable `.schema.json` existed for Cognitive Run Record or Capability Decision Record; the adapter schema allowed unknown fields | implementation required: strict schemas and deterministic validators |
| E — model/grader blind spots | current local Gemma run passed 29/29; pre-existing local artifacts recorded Qwen SUT runs of 27/29 and 28/29, and the runner had no reliable truncation/identity checks | implementation required: independent configuration, deterministic checks and multi-model report |
| F — version drift | `skills/cognitive-os/VERSION` was stable `1.4.0`, while `gemini-extension.json` and `CONTRIBUTING.md` still contained `1.4.0-dev`; Agent Skills docs also described dev metadata | implementation required: synchronized `1.5.0-dev` development metadata while preserving v1.4 historical evidence |
| session/run binding | current harness selected the newest exported session by timestamp and did not require a command correlation marker | implementation required: host-observed run correlation |
| mutation detection | current H14-E05 depended primarily on tool-name/prompt pattern checks | implementation required: scoped before/after fingerprints and explicit unknown state |

## Gate 0 status

The two reproduced fail-open paths now have regression coverage and pass their focused tests. Gate 0 remains open until candidate evidence binding, contract hardening, model/grader checks and version synchronization are implemented and the full regression is rerun.
