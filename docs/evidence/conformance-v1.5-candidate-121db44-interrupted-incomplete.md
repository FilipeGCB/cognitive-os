# Interrupted V1.5 conformance run — `INCOMPLETE`

This record preserves the observable progress of the Qwen SUT → Gemma grader
run that was intentionally stopped before the checkpoint-capable runner was
implemented. It is not a release result and is not a PASS.

- candidate SHA: `121db44f4e57a664460a1e1265087df0a168d19f`;
- SUT: `qwen3.8:27b`;
- grader: separate `gemma4:26b-a4b-it-qat` invocation;
- suite: V1.5, 58 cases;
- status: `INCOMPLETE`;
- confirmed completed case markers: `CD-01` through `CD-10`, `RS-01` through
  `RS-04`;
- `RS-05` had started when the process was interrupted, but completion and
  response state were not observed in a persisted report;
- confirmed completed SUT/grader pairs: 14 cases / 28 model calls;
- final score, critical-failure aggregate and release eligibility: **not
  computed** for this interrupted run.

The old runner only wrote its JSON report after the entire loop, so no partial
response file was generated by that process. Existing temporary reports were
left untouched. The redesigned runner uses per-case SUT and grade cache
records plus atomic checkpoints; a future interruption will retain every
completed response without representing the incomplete run as PASS.
