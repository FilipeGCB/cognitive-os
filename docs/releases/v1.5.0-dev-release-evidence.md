# Cognitive OS V1.5 development release evidence

Status: development candidate; not a stable release and not published as a
release.

The machine-readable source of truth is
[`v1.5.0-dev-release-evidence.json`](v1.5.0-dev-release-evidence.json). It
binds the evidence to candidate
`228046c1ca46a126f472dc0e87e73ad083b1fb77`, version `1.5.0-dev`, the exact
canonical skill-tree fingerprint, manifest and harness hashes, and a
host-observed Hermes run.

## Gate summary

| Gate | State | Boundary |
| --- | --- | --- |
| Gate 0 baseline hardening | PASS | V1.4 findings reproduced or formally bounded and regression-tested |
| Gates 1–7 | PASS | contracts, governance, local flight recorder, evals and distribution checks |
| Gate 8 multi-model conformance | PASS | primary 58/58 plus independent Qwen SUT critical coverage 14/14 |
| Gate 9 Hermes E2E | BLOCKED | fresh aggregate is not a release PASS; MCP and NotebookLM were not callable |
| Gate 10 Work | UNAVAILABLE | exact smoke procedure exists; runtime surface was not available |
| Gate T public telemetry | PASS / sharing unavailable | client controls pass; default is `OFF` and no endpoint is enabled |
| Gate TC collector | PARTIAL | private source/tests exist; no deployed endpoint is claimed |

The deterministic repository suite passed `157/157`. The V1.4 behavioral
regression passed `29/29` with no critical failures. The V1.5 primary model
run passed `58/58` with Gemma as SUT and an independent Qwen grader. Qwen was
also tested as SUT on all 14 critical cases, with an independent Gemma grader,
and passed `14/14`; its report remains globally `INCOMPLETE` because the
selection is intentionally critical-only.

The V1.5 release-gate model work used 144 calls before cache hits: 116 for the
complete primary matrix and 28 for the independent critical-only matrix. The
runner retains per-case SUT responses and does not call the SUT again when
only the grader changes and all cache inputs still match.

The candidate is not a stable release. The remaining release boundary is the
blocked/ unavailable host evidence, not a silently weakened critical gate.
