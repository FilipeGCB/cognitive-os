# Cognitive OS v1.4.0 — Release Evidence

Date: 2026-09-04
Status: release gates passed on promotion branch; merge/main verification and immutable release remain pending explicit approval
Repository: `FilipeGCB/cognitive-os`
Branch: `feat/v1.4-public-foundation`
PR: `#1`

COGNITIVE_GATE: PASS
E2E_GATE: PASS
RELEASE_GATE: PASS

## Scope

This record supports promotion of the first public Cognitive OS release. It does not treat historical private V1.3 evidence as proof of V1.4, and it separates deterministic/package CI, behavioral conformance, live capability execution, and stable promotion.

## Behavioral cognitive conformance

The public V1.4 suite contains 29 cases: 19 core behavior/capability-routing cases and 10 output/editorial cases. The release threshold is at least 27/29 with zero failures among the declared critical cases.

Observed final maintainer results:

- behavioral candidate SHA: `9ffe814c13820d37abae0e6d9d4e4e19dc447419`;
- SUT: `gemma4:26b-a4b-it-qat` through local Ollama;
- separate same-family Gemma grader: `PASS`, `29/29`, critical failures `[]`;
- independent cross-grader: `qwen3.8:27b`, `PASS`, `29/29`, critical failures `[]`;
- grader disagreements: `[]`.

The local provenance files are:

- `evals/runs/v1.4-gemma4-26b-conformance-final.json`;
- `evals/runs/v1.4-gemma4-26b-crossgrade-qwen38-27b-final.json`.

These files are maintainer-machine evidence and are not silently represented as repository-hosted artifacts.

### Critical Audit regression

The V14-C12 Audit failure was traced to a universal generation budget that truncated the observable audit response and to grader ambiguity between permitted Cognitive Run Records and hidden chain-of-thought. Targeted regression tests were added before the harness correction. After the fix, V14-C12 passed with both graders and the full suite passed 29/29.

Therefore:

`COGNITIVE_GATE: PASS`

## Live host/capability E2E

Final same-candidate maintainer run:

- candidate SHA: `f7b690bfaead92a7459c16dfd12f1b5469778ccc`;
- host: Hermes Agent `v0.20.0 (2026.8.3)`;
- isolated surface: `cli-profile:cognitive-os-e2e`;
- SUT: local `gemma4:26b-a4b-it-qat` through Ollama/custom OpenAI-compatible endpoint;
- six case records were cleared and regenerated on the same candidate;
- `uniform_candidate=True`;
- `all_six_pass=True`;
- `E2E_GATE=PASS`;
- `FINAL_GATE2_VALID=True`;
- verifier exit: `0`.

| Case | Capability/path | Availability | Invocation | Result | State | Pass |
| --- | --- | --- | --- | --- | --- | --- |
| H14-E01 | host capability discovery / local model preflight | AVAILABLE | CALLED | SUCCESS | EXECUTED | true |
| H14-E02 | live Web Search | AVAILABLE | CALLED | SUCCESS | EXECUTED | true |
| H14-E03 | Hermes MCP discovery/connection | AVAILABLE | CALLED | SUCCESS | EXECUTED | true |
| H14-E04 | NotebookLM Grounded Corpus Research | AVAILABLE | CALLED | SUCCESS | EXECUTED | true |
| H14-E05 | untrusted external instruction / authorization boundary | AVAILABLE | CALLED | SUCCESS | EXECUTED | true |
| H14-E06 | intentionally unavailable capability / bounded fallback | UNAVAILABLE | NOT_CALLED | UNAVAILABLE | UNAVAILABLE | true |

### H14-E04 authenticated Grounded Corpus Research

The final E04 used explicit account-use approval and a preconfigured isolated NotebookLM MCP. The profile selected only these read-oriented tools:

- `notebook_list`;
- `notebook_describe`;
- `source_list`;
- `source_read`.

The auth check reported `status=ok`, `token_fetch=true` and `sid_cookie=true`. Hermes connected successfully to the NotebookLM MCP. The actual chat used toolsets `skills,notebooklm`, and the session export observed NotebookLM calls including `notebook_list`, `source_list` and a successful `source_read` result. The harness set `grounded_source_read_observed=True`.

No NotebookLM write capability is claimed as tested or authorized. NotebookLM remains an optional account-bound candidate implementation of abstract Grounded Corpus Research, not a Cognitive OS core dependency or bundled/default requirement.

### E2E truth discipline

The harness records `availability`, `invocation` and `result` separately and does not treat assistant prose alone as proof of execution. Only `AVAILABLE + CALLED + SUCCESS` supports an unqualified execution claim. H14-E06 intentionally demonstrates that a missing capability remains visible as `UNAVAILABLE + NOT_CALLED + UNAVAILABLE`.

Therefore:

`E2E_GATE: PASS`

## Deterministic/package CI

Successful CI on the same E2E candidate:

- run: `33858086502`;
- head: `f7b690bfaead92a7459c16dfd12f1b5469778ccc`;
- conclusion: `success`;
- `test`, `install-smoke`, `notebooklm-adapter-smoke`, `secret-scan`: all success.

Final promotion-branch verification after stable metadata, stable-version test update, and removal of the temporary Jules workflow:

- promotion SHA: `5fd7b6c7097d981925989b061b90f032bc2c4751`;
- workflow: `ci`;
- run: `33865224275`;
- conclusion: `success`;
- `test`: success;
- `install-smoke`: success;
- `notebooklm-adapter-smoke`: success;
- `secret-scan`: success.

The successful `test` job covered unit/contract tests, E2E harness regressions, eval-manifest validation, public-package validation, bounded public PII scanning, Decision Brief renderer smoke, Python compile checks, and the explicit stable version contract `1.4.0`. The install smoke verified Agent Skills discovery/install for Codex, Claude Code and Gemini CLI. The NotebookLM smoke verified pinned package/entrypoint installability. Gitleaks scanned repository history.

## Packaging and harness defects found by the gates

The gates caught material issues before release, including:

1. a distribution-contract mismatch in OpenAI documentation tests;
2. invalid SKILL.md YAML frontmatter rejected by the Agent Skills CLI;
3. incomplete behavioral-eval fixtures;
4. insufficient Audit generation budget and grader ambiguity;
5. Hermes MCP negative outcomes returning shell exit zero, requiring semantic classification;
6. an unsafe/interactively configured NotebookLM MCP E2E path, changed to fail closed and require preconfiguration;
7. a brittle dynamic MCP Tool Search path for the local Gemma SUT, replaced in the isolated E2E profile by eager tool exposure;
8. incorrect MCP toolset filtering, corrected to use the actual configured server name `notebooklm`;
9. a development-only version assertion that correctly blocked the first `1.4.0` promotion attempt until the stable-version contract was updated.

Targeted RED/GREEN regression cycles were used for the later Hermes/NotebookLM harness fixes. The stable-version promotion failure was also observed directly in CI before its minimal test-contract correction.

## License and companion status

Cognitive OS is licensed under Apache License 2.0; the root `LICENSE` file is authoritative.

No third-party companion is bundled or promoted as a universal default. The authenticated NotebookLM path is runtime-validated only for the bounded read-only maintainer E2E described above and still requires specific consent and account authentication.

## Stable promotion metadata

Promotion metadata is staged and verified on `feat/v1.4-public-foundation`:

- `skills/cognitive-os/VERSION`: `1.4.0`;
- `CHANGELOG.md`: `v1.4.0 — 2026-09-04`;
- `README.md`: reflects observed conformance/E2E state and the remaining merge/main-release boundary;
- stable version contract test expects exactly `1.4.0`;
- temporary Jules release-gate workflow removed;
- Jules PR #3 closed without merge as stale/superseded;
- Jules issue #2 closed as completed/superseded by PR #1;
- release workflow remains downstream of successful `main` CI, requires the exact line `RELEASE_GATE: PASS`, refuses dev/rc versions, refuses an existing stable tag/release, and targets the exact verified `main` SHA.

## Final promotion record

Observed release-gate state:

- cognitive conformance: `PASS` / 29/29 Gemma / 29/29 independent Qwen;
- cognitive critical failures: `[]`;
- cognitive grader disagreements: `[]`;
- live host/capability E2E: `PASS` / 6/6 on one candidate;
- E2E candidate CI: `33858086502` / `success`;
- final promotion SHA: `5fd7b6c7097d981925989b061b90f032bc2c4751`;
- final promotion CI: `33865224275` / `success`;
- stable version metadata: `1.4.0`;
- explicit release gate: `PASS`;
- PR merge: `PENDING USER APPROVAL`;
- downstream main CI: `PENDING MERGE`;
- immutable tag/release: `PENDING GREEN MAIN CI`.

`RELEASE_GATE: PASS` means the repository-defined pre-merge promotion gates have observable support. It does **not** authorize merging the PR by itself. Merge to `main` remains explicitly user-controlled; only a successful downstream `main` CI may trigger creation of the immutable `v1.4.0` tag/release.
